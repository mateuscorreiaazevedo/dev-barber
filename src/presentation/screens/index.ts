import { lazy } from 'react'

export const PreloadScreen = lazy(() => import('./preload'))
export const SignInScreen = lazy(() => import('./sign-in'))
export const SignUpScreen = lazy(() => import('./sign-up'))
